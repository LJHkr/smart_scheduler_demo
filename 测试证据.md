# 智能排班 Agent——测试证据

## 测试结论

以下测试覆盖正常排班、局部排班以及“无法判断”边界。自动测试命令：

```powershell
python -m unittest discover -s tests -v
```

## 用例 1：完整一周正常排班

- **输入**：`请生成下周完整排班，尽量满足员工偏好。`
- **预期输出**：生成周一至周日、每天早晚班，共 14 个班次。
- **验证方式**：逐班和逐人检查人数、技能、请假、工作日期、每周班次、连续工作及晚班接早班。
- **实际结果**：成功生成 14 个班次，R-01～R-09 全部通过。
- **是否正确**：是。
- **自动测试**：`test_full_week_has_valid_schedule`

## 用例 2：只生成周末排班

- **输入**：`只生成周末排班。`
- **预期输出**：只包含周六、周日的早班和晚班，共 4 个班次；每班至少 6 人。
- **实际结果**：请求被解析为周六和周日，生成结果通过规则检查。
- **是否正确**：是。
- **自动测试**：`test_weekend_request_is_parsed`

## 用例 3：“无法判断”边界 case

- **输入**：`请根据下周客流量安排人手，高峰多排、低峰少排。`
- **预期输出**：不生成排班，明确回复无法判断，并要求补充各日期、各班次的客流预测或每班目标人数。
- **实际输出**：`无法判断：题目没有提供各日期、各班次的客流量或业务量数据。请补充各日期、各班次的客流预测，或直接给出每班目标人数。`
- **判断依据**：题目虽然提到根据业务量排班，但提供的数据中没有业务量、客流预测或业务量与人数的换算关系。如果直接生成会引入未经提供的数据。
- **是否正确**：是。系统没有编造客流数据，也没有擅自决定高峰期人数。
- **自动测试**：`test_missing_business_volume_returns_unable_to_determine`

## 补充边界

以下请求同样应返回“无法判断”：

- `优先安排表现最好的员工。`——缺少绩效或能力排名。
- `按最低人工成本排班。`——缺少工资、时薪或用工成本。

这些信息不能从岗位或技能字段中推断，避免违反“技能和事实必须来自员工数据”的原则。
